import React from 'react';
import { Charts } from '../components/Charts';

/**
 * Charts page component
 * This is a simple wrapper around the main Charts component
 */
const ChartsPage = () => {
  return <Charts />;
};

export default ChartsPage;